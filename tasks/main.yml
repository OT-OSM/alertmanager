---
# tasks file for alertmanager
- name: Install Alertmanager
  include: install.yml

- name: Configure Alertmanager on Debian
  include: debian.yml
  when:
    ansible_os_family == "Debian"

- name: Configure Alertmanager on RedHat
  include: redhat.yml
  when:
    ansible_os_family == "RedHat"
